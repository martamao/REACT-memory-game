## 1. Layout Styling

- [x] 1.1 Update `src/App.scss` so the game view can use a large-screen grid layout with title/counter above the board and result controls beside the board.
- [x] 1.2 Adjust `.table` sizing in `src/App.scss` so the board can grow on large screens while preserving `aspect-ratio: 1 / 1`.
- [x] 1.3 Style `.resultSection` and `.resultButtons` for a compact side column on large screens and a stacked layout on small screens.

## 2. Constraints and Verification

- [x] 2.1 Confirm no React, hook, constants, or game logic files are modified.
- [x] 2.2 Verify the in-progress game view keeps the board centered before result buttons exist.
- [x] 2.3 Verify the finished-game view places the board and result buttons side by side on large screens.
- [x] 2.4 Verify small screens keep the existing stacked layout.
